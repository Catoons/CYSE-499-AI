A convolutional neural network (CNN) is a neural network that learns features via filter (or kernel) optimization. This CNN is trained to identify the numbers 0-9 in images.

TRAINED_NUMBER_IDENTIFIER_CNN.pth is the trained model. It has 98.46% accuracy with the seed and dataset used. 
